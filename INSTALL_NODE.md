# Node.js 安装指南

## 🔍 问题
系统找不到 `npm` 命令，需要先安装 Node.js。

## ✅ 解决方案（选择一种）

### 方法 1：修复 Homebrew 权限后安装（推荐）

如果您的 Homebrew 权限有问题，先修复：

```bash
sudo chown -R $(whoami) /opt/homebrew/Cellar
```

然后安装 Node.js：

```bash
brew install node
```

### 方法 2：使用 Homebrew 直接安装（如果权限正常）

```bash
brew install node
```

### 方法 3：从官网下载安装（最简单）

1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载 **LTS 版本**（推荐，更稳定）
3. 运行下载的 `.pkg` 安装包
4. 按照安装向导完成安装
5. 重启终端

### 方法 4：使用 nvm（Node Version Manager）

如果您想管理多个 Node.js 版本：

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重启终端或运行
source ~/.zshrc

# 安装最新 LTS 版本的 Node.js
nvm install --lts

# 使用该版本
nvm use --lts
```

## ✅ 验证安装

安装完成后，在终端运行：

```bash
node --version
npm --version
```

如果显示版本号（例如 `v20.x.x` 和 `10.x.x`），说明安装成功！

## 🚀 安装完成后

1. **进入项目目录**：
   ```bash
   cd "/Users/poonrealiam/Desktop/My 1st NFNJ app/nofap-nojerk-extracted"
   ```

2. **安装项目依赖**：
   ```bash
   npm install
   ```
   这可能需要几分钟时间，会下载所有必需的包。

3. **启动开发服务器**：
   ```bash
   npm run dev
   ```

4. **打开浏览器**：
   访问 `http://localhost:3000`（或终端显示的地址）

## 📝 注意事项

- **首次安装**：`npm install` 可能需要 2-5 分钟
- **端口占用**：如果 3000 端口被占用，Vite 会自动使用下一个可用端口
- **环境变量**：确保 `.env.local` 文件已配置好 Supabase 和 Gemini API 密钥

## 🆘 如果遇到问题

### 问题：`command not found: npm` 仍然出现
**解决**：
- 重启终端
- 检查 Node.js 是否在 PATH 中：`echo $PATH`
- 尝试使用完整路径：`/usr/local/bin/npm` 或 `/opt/homebrew/bin/npm`

### 问题：权限错误
**解决**：
```bash
sudo chown -R $(whoami) /opt/homebrew
```

### 问题：npm install 很慢
**解决**：
- 使用国内镜像（可选）：
  ```bash
  npm config set registry https://registry.npmmirror.com
  ```

## 🎉 完成！

安装 Node.js 后，您就可以运行 `npm install` 和 `npm run dev` 了！
